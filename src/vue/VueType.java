package vue;

public enum VueType {
    // VuePerspective qui est à gauche. Cela permet de l'identifier pour le gestionnaire de commande.
    GAUCHE,
    // VuePerspective qui est à droite. Cela permet de l'identifier pour le gestionnaire de commande.
    DROITE
}